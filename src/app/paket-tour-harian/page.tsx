import { CategoryPage, categoryMetadata } from "@/components/site/CategoryPage";

export const metadata = categoryMetadata("paket-tour-harian");

export default function Page() {
  return <CategoryPage slug="paket-tour-harian" />;
}
